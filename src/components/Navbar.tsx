import { Box, Typography } from '@mui/material';
import React from 'react';

const Navbar = () => {
  return (
    <Box width="100%" paddingBottom="4rem">
      <Box width="100%" display="flex" position="fixed">
        <Typography> navbar </Typography>
      </Box>
    </Box>
  );
};

export default Navbar;
