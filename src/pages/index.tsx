import { Box, Typography } from '@mui/material';

const Home = () => {
  return (
    <Box height="100%">
      <Typography variant="h1"> Hello world</Typography>
    </Box>
  );
};

export default Home;
